#!/usr/bin/env bash
# family-travel-planner 所需凭证（示例）。
#
# 真实文件位于 ~/github/my_dot_files/secrets.sh —— 脚本运行时会 `source` 它注入 env
# （可用环境变量 SECRETS_FILE 改路径）。把下面内容加进你的 secrets.sh，
# ⚠️ 真实 Key 绝不提交进任何仓库。
#
# 高德 API Key 申请：https://lbs.amap.com/dev/key/app（选「Web服务」类型）

export GAODE_API_KEY="your_api_key_here"
